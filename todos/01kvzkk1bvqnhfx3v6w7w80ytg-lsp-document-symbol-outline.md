# LSP heading outline via textDocument/documentSymbol (tier 2)

Deferred tier-2 ntropy language-server feature.

Provide a heading outline for the open note via `textDocument/documentSymbol`,
enabling in-editor structure navigation and breadcrumbs. Requires light
Markdown heading scanning of the buffer (ATX `#` headings), not a full parser.
</content>
